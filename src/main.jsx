import React from "react";
import ReactDOM from "react-dom/client";
import InvoiceStudio from "./InvoiceStudio.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <InvoiceStudio />
  </React.StrictMode>
);
